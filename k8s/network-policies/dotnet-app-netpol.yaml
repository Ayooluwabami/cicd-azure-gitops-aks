apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: dotnet-app-ingress
  namespace: microservices
spec:
  podSelector:
    matchLabels:
      app: dotnet-app
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - port: 8080
          protocol: TCP
